import React from 'react';
import {Grid} from "@mui/material";

const TabTwoCard = ({error, flag}) => {
    return (
        <Grid container spacing={1}>
            <Grid item xs={12} sm={12}>
                <Grid container spacing={2}>

                </Grid>
            </Grid>
        </Grid>
    );
};

export default TabTwoCard;
